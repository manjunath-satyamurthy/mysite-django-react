import React from 'react';
import {shallow} from 'enzyme';
// import Home from '../src/main/scripts/Home.js';


test('Home component renders an ProfilePhoto', () => {
});

test('Home component has an MainHeading', () => {
});

test('Home component renders TextDescription', () => {
});

test('Home component renders ProfileLinks', () => {
});